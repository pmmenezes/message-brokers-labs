apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: strimzi-kafka-dashboard
  namespace: monitoring
  labels:
    app: grafana
    grafana_dashboard: "1"
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  configMap:
    name: strimzi-kafka-dashboard
    key: strimzi-kafka-dashboard.json
